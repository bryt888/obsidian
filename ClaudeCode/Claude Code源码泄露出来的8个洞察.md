
大家好，今天我们来聊聊Claude Code源代码泄露事件，以及从中能得到的8个关键洞察。
 
Claude code的源代码刚刚在互联网上被泄露了，包含2000个文件，超过50万行的生产代码，也就是Claude code背后的实际代码库。
 
事情是这样的，Anthropic发布了一个npm包，其中包含了一个源映射文件，指向他们服务器上可读的typescript源代码。一位安全研究员发现了这一点，顺藤摸瓜几小时内完整的源代码就在GitHub上被公开镜像了。这件事迅速在网络上疯传，所以我梳理了大家发现的所有内容，仔细研究了整个代码库，并提炼出了每一个能真正改变你使用Claude code方式的实用见解。
 
我说的是那些大多数人根本不知道存在的隐藏命令、上下文和token在底层的实际工作方式以及架构，揭示了哪些大多数用户从未接触过的功能。我把这些内容拆解成了8个关键见解，今天我们会一一讨论。
 
每一个见解都会在前一个的基础上深入，前面几个见解会改变你对这个工具的看法，但最后一个见解会把所有内容串联成一个系统，而这才是真正的杠杆所在。所以在看完这个视频后，你就能像顶尖1%的用户一样使用Claude code。
 
那我们现在就开始吧。好的，我得先来一个简短的免责声明，在这个视频里我不会向大家展示任何实际的源代码，如果你自己去下载也请务必小心。
 
尽管这些内容是通过一个公开的NPM包泄露出来的，但版权仍然归Anthropic所有，他们已经表明愿意发出DMCA删除通知。所以我所讲的内容只是关于你如何更好的使用这个工具的心得体会。
 
好的，那我们先快速开始，给大家感受一下这个代码库里都有什么。你知道在Claude code思考时会看到那些小提示消息吗？那其实是一个包含了187个旋转动词的文件，里面有像computing计算或generating生成这样常规的词。
 
但也有一些有趣的，比如boondoggle瞎忙、discombobulating搞糊涂、fibertigibbing胡言乱语、moonwalking月球漫步之类的。Anthropic专门雇人写了这187个词，这就是我希望开发者拥有的尽头。总之这只是个开胃菜。
 
我们来聊聊这里真正重要的内容，这并不是你想的那样。这次泄露显示Claude code并非大多数人以为的终端版Claude机器人，仅能访问本地文件，这种理解并不准确。
 
源代码显示Claude code实际上是一个完整的智能体运行时环境，它是一个用Bindingscript和react构建的正式应用程序。它拥有一个工具系统、命令系统、记忆系统、权限引擎、任务管理器、多智能体协调器以及一个MCP客户端和服务器，这些都被整合在同一个执行流程中。
 
流程是这样的，你的输入首先会经过一个CLI解析器，然后进入一个叫做查询引擎的模块，调用LLM API运行工具执行循环，最后把结果渲染回你的终端。
 
我知道这听起来像是一堆术语，那么这对你来说有什么意义？因为如果你只是把Claude code当做聊天机器人来用，也就是说你只是输入问题并期待得到好的答案，那你实际上只用了它大约10%的功能，其余的价值都体现在围绕模型构建的那些系统上，而这正是本视频接下来要讲的内容。
 
见解2：你忽略的命令服务
 
有一件事是大多数人不了解的，而Claude code的源代码揭示了这一点。Claude code里大约有85个斜杠命令，大多数用户可能只知道其中5个左右，你可能只会输入 help ，差不多就这样了，或者也许还有 context ，但那些高级用户也就是能从Claude code获得10倍价值的人，他们把这些命令当做快捷方式来用。
 
以下是最重要的一些命令：
 
-  /init ：这个命令可以设置你的项目上下文，它会生成一个Claude MD文件，这个文件相当于Claude code在你的代码库中的操作手册，我们马上会详细讲讲为什么这个文件如此重要。
-  /plan  &  /ultraplan ：这些命令会让Claude code进入规划模式，它不会立刻执行操作而是会先规划出完整的方案，然后在开始修改文件之前征求你的同意。当你在处理复杂项目时这一点非常重要，因为你不希望它随意开始到处编辑文件，从长远来看这样做还能帮你节省tokens。
-  /compact ：这个命令对于省钱来说非常重要。当你的对话变长时保留上下文会变得很昂贵，所以 compact 会压缩你的对话历史，让你保留重要内容同时去除无关信息。如果你发现tokens消耗很快，这是最快的解决办法之一。你还可以在 compact 后面加上一个提示词，比如如果你想压缩一个项目但又想保留所有关于网站集成的重要信息，你可以在执行压缩时进行指定。
-  /review  &  /security-review ：这些都是内置的代码审查工作流程，你无需再让Claude code查看你的代码，只需使用这些命令它就会执行结构化的审查。而这些作为专门命令的存在也说明了在这个产品中代码审查并不是事后才考虑的，这是一个一流的工作流程。
-  /context ：这个命令用于管理Claude code实际关注的是哪些文件，这很重要。因为每一个被纳入上下文的文件都会消耗你的tokens，所以这个命令可以让你更清楚地了解情况，帮助你整理和清理你的项目。
-  /cost ：这个命令会显示你实际花费了多少，大多数人在看到账单之前通常都不知道一次会话到底花了多少钱，所以运行 cost 。
-  /resume  &  /summary ：这些命令可以让你在不同会话之间无缝衔接，无需重新解释所有内容。
 
而这些内置命令只是冰山一角，重点是更好的提示词是一种杠杆，但了解这些命令则是另一种完全不同的杠杆，而大多数人甚至都没有用到它。
 
见解3：记忆系统比你想的更重要
 
这里的源代码显示Claude code拥有一个完整的记忆系统，而其核心就是一个名为Claude MD的文件。现在大多数人要么忽略这个文件，要么随意往里面塞一堆杂乱的笔记，这其实是个大错误。因为Claude Md不是文档而是操作上下文，所以你可以这样想：如果说Claude code是一名员工，那么Claude MD就是他们的入职手册，他会告诉他们我们是这样做事的，这些是我们重视的事情，这些是我们绝对不会做的，还有这个项目的结构是这样的。
 
最优秀的用户会把Claude MD保持简洁，有主见且实用。比如“始终使用typescript的strict模式”“测试文件放在与原文件相邻的test文件夹里”“修改数据库结构前必须先运行迁移”“使用NPM/不用NPM”之类的内容决策规则约束约定，而不是写一部关于你项目历史的小说。
 
源码还显示这里面其实有多个层次，你会发现有用户级别的记忆，提取的记忆，甚至还有团队记忆同步。这告诉我们Claude code在项目、用户和会话上下文中都具备持久化记忆机制，而Claude MD是影响其未来会话行为的最重要输入之一。
 
所以如果你只从这段视频中记住一件事，那就是今天就去更新你的Claude MD文件，无论是在全局层面还是项目层面，而且要记住这些内容会在每一次会话、每一次聊天之前被注入。
 
见解4：权限问题是运行缓慢的元凶
 
用过Claude code的人都遇到过，你让他做事，他总不停地问“能运行吗？”“能编辑吗？”“能授权吗？”这确实挺烦人的。多数人以为靠提示词，核心其实是权限设置。源码显示其深度权限系统包含多种模式：默认模式事事询问、计划模式以及绕过或自动模式则直接执行。
 
但这才是亮点，你可以设置通配符权限，比如“允许所有git命令”或者“允许在我的SRC文件夹中编辑所有文件”。所以Claude code不会再反复问你15遍“我可以运行git status吗？”“我可以编辑这个文件吗？”“我可以运行这个测试吗？”你只需要设置一次规则，之后它就能自动运行了。
 
对于那些重复性的工作流程，也就是你每天都要做的事情，这是你能做出的最具投资回报率的改变之一。
 
所以在你的 settings.local.json 或 settings.json 文件中，你可以为你总是想要允许、总是拒绝，或者总是需要询问的操作，设置全局用户级或项目级的权限。这样你就可以从事无巨细的监管每一个操作，转变为真正让他像一个代理一样自主运行，而你可以暂时离开。
 
见解5：为多智能体协作而设计
 
这一点让我很惊喜，源代码中展示了一个完整的协调子系统，包括智能体工具、团队工具，以及专为后台和并行工作设计的任务系统。
 
用通俗的话来说，这意味着其架构显然是为了支持任务分解而设计的，可以将工作分配给多个能够并行运行的智能体。比如说，一个智能体负责探索你的代码库，另一个负责实现更改，还有一个负责验证测试，甚至在源代码中还有后台任务的概念，也就是在你专注于其他事情时，工作仍能持续进行。
 
目前这些功能中，有多少是完全面向用户的，又有多少还在逐步推出，仅从源代码来看，这点有些难以判断，但架构已经明确表达了其意图。Claude code的设计目标是通过将复杂的多步骤工作拆解开来处理，而不是把所有任务都塞进一个线程里。
 
这里的实际启示在于，你应该如何组织你的请求，不要用一个庞大的提示，比如“重构整个模块，更新测试并修复文档”，而是要把这些任务拆分开来，让Claude code来分解这些工作。你可以像区分工作流和代理那样来思考这个问题，最好的结果来自于将复杂的工作拆解为清晰的顺序步骤，或者在这种情况下是并行步骤，而不是指望一个庞大的通用指令，交给一个庞大的通用代理，就能把一切都做好。
 
见解6：MCP、插件和技能是真正的扩展层
 
如果你一直关注我的频道，你就知道我经常谈到MCP，也就是模型联系人协议，而源代码也证实了这一点。
 
MCP不仅仅是被Claude code支持，它实际上已经深度集成进了架构中。Claude code既是MCP客户端，也是MCP服务器，它可以通过MCP连接到外部工具，其他系统也可以连接到它。但它确实不仅仅局限于MCP，源代码还揭示了一个技能和插件层。
 
这意味着高级用户可以构建可重复的工作流，自定义功能以及随时间积累的特定领域扩展。这不仅仅是连接某一个工具，而是围绕Claude code构建一个生态系统，让它在你的具体工作中变得更智能。这正是Claude code不再只是一个编程工具，而开始成为一个集成层的地方。
 
你可以把它连接到数据库、API、内部工具、文档系统，任何带有Claude code服务器的东西，然后你还可以在其基础上叠加技能和插件，用于你经常重复的工作。现在你连接的系统越多，它就变得越有用。所以当然强大之处在于工具本身，关键在于你连接了什么，以及你开始用它自动化和构建的工作流。
 
见解7：有一些功能我们尚未能使用
 
这一点非常有趣，因为代码库中包含了对一个叫做 user type 的检查，其中一个取值是 ant ，意思是anthropic。这告诉我们某些功能实际上被内部的功能开关所限制。
 
源码中提到了诸如语音模式、一个叫做Kyrus的系统守护进程模式以及协调器模式，这些都被功能开关所控制，暗示他们要么是内部使用，要么是实验性的或者可能正在逐步推出。
 
我们无法确定内部体验到底有多大不同，其中一些可能只是早期原型，有些可能已经接近公开发布了，仅凭源码本身无法得知这些信息。但它确实证实了Claude code里有大量的功能开关，这意味着不同用户根据它们的环境构建版本或分发组可能已经获得了实质上不同的体验。
 
实际的结论是要密切关注Claude code的更新，那些目前被标记或受限的功能很可能已经在开发计划上了。而当他们上线时，像你这样已经了解其架构的人将能够立即上手使用。想想看Claude code最近推出了多少新功能和工具，这是因为这些功能早就在内部开发和测试过了。
 
见解8：如何利用这些功能省钱并获得更好结果
 
让我把这些内容串联起来，讲讲那些让顶尖1%用户脱颖而出的习惯：
 
- 把Claude MD当做增效器：保持简洁、定期更新并引导到其他文件，因为这一个文件决定了你与Claude code的每一次交互。
- 学习命令界面：你不需要掌握全部85个命令，但像 plan 、 compact 、 cost 和 resume 这些命令要会用，它们会立刻改变你的日常工作流程。
- 为重复性工作流配置权限：不要再对每一个操作进行确认，为你每天要做的事情设置通配规则。
- 用分解的思维方式：把复杂的工作拆分为搜索阶段、规划阶段、执行阶段和验证阶段，让Claude code按照它本来的设计方式工作。
- 像管理金钱一样管理上下文：当对话变长时，使用 compact ；使用 context 来控制加载的内容；使用 summary 和 resume 在不同会话之间传递工作。因为context中每一个不必要的文件都是你要为之付费的tokens，而且上下文管理不仅仅是锦上添花，这是你需要养成习惯的事情。
- 让它连接到各种事物：无论是MCP服务器、CLI插件还是技能，Claude code能访问的工具越多，它的价值就越大。
- 把它当做基础设施来看待：源代码揭示了庞大的配置界面、子代理模型覆盖、shell行为、隐私控制，甚至可以通过不同的云后端如AWS Bedrock或Google Vertex进行路由。大多数人从未接触过这些设置，但如果你认真使用Claude code，调整环境以适应你的工作流程，会带来真正的优势。